name: burst2safe
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python
  - pip
  - gdal
  - numpy
  - lxml
  - tifffile
  - asf_search
  # For packaging, and testing
  - pytest
  - pytest-cov
