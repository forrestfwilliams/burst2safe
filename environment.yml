name: burst2safe
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python
  - pip
  - gdal
  - shapely>=2
  - numpy
  - lxml
  - tifffile
  - asf_search
  # For packaging, and testing
  - pytest
  - pytest-cov
