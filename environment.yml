name: burst2safe
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - pip
  - gdal
  - numpy
  - lxml
  - asf_search
  - crcmod
  # For packaging, and testing
  - pytest
  - pytest-cov
